#include "Person.h"

int main()
 {
	int n, tz;
	char name[MAX_SIZE];

	cout << "Enter num of pairs: ";
	cin >> n;
	cout << "\nEnter you pairs, name len max 20, each line pair\n";
	cin >> tz;
	cin.ignore();
	cin.getline(name,MAX_SIZE);



}